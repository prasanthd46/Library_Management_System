# ------------------------------------------------
# JWT (JSON Web Token) Configuration
# ------------------------------------------------
# This file serves as a template. Copy it to ".env" and fill in your actual values.
# The real ".env" file should NOT be committed to version control.

# A long, random, secret string used to sign JWTs.
# Generate a new one for project 
SECRET_KEY="change_this_to_a_very_long_and_random_secret_string"

# The algorithm to use for signing JWTs. HS256 is the standard.
ALGORITHM="HS256" (Generic-Choice)

# How long an access token should be valid (in minutes).
ACCESS_TOKEN_EXPIRE_MINUTES=30 (Any Number as per the requirement)
